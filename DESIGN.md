# Badwater Ignition — Design System

A fast, offline field calculator for **Probability of Ignition (PIG)**, **Fine
Fuel Moisture (FFM)**, and **relative humidity**, built on the NWCG Incident
Response Pocket Guide (IRPG, PMS 461) and belt-weather-kit tables.

This document is the source of truth for the visual system and is meant to be
picked up and finalized in **Claude Design**. The calculation engine and screen
structure are already built (SwiftUI + the `BadwaterCore` package); what remains
is visual finalization — refining the palette on real devices, the app icon, and
motion.

---

## 1. Brand story — Badwater Basin

The app is named for the hottest, driest, lowest place in North America — the
exact conditions this tool measures. Its most striking feature is the **teal,
spring-fed pool sitting against blinding white salt**. That contrast is the
identity:

- **Pool teal** is the one brand accent. It is water in a place defined by its
  absence — calm, trustworthy, instrument-like.
- The **entire warm range (amber → orange → red) is reserved for fire-behavior
  severity.** A fire app that spends orange on decoration has nothing left to
  signal danger. When the screen turns orange, it *means* something.
- Two grounds: **salt-flat** (light) and **basin-night** (near-black dark, to
  protect dark adaptation on the line).

Identity comes from **color, spacing, and an instrument-like layout** — not a
custom typeface. Type is the native system stack, per Apple platform
conventions.

---

## 2. Color tokens

Defined in `App/BadwaterIgnition/Assets.xcassets/Colors` (generated by
`scripts/generate_color_assets.py` — edit there and regenerate). Every token has
a light and dark value.

### Grounds & surfaces
| Token | Light | Dark | Use |
|---|---|---|---|
| `BasinBackground` | `#F2F0E9` | `#0F0E0B` | Screen background |
| `Surface` | `#FFFFFF` | `#191712` | Cards, inputs |
| `SurfaceRaised` | `#FAF9F4` | `#201D16` | Elevated surfaces |
| `SurfaceSunk` | `#EAE8DF` | `#14120D` | Chain strip, interpretation panel |

### Text & lines
| Token | Light | Dark | Use |
|---|---|---|---|
| `Ink` | `#211E17` | `#F0ECDF` | Primary text, values |
| `Muted` | `#6E6A5D` | `#9A9484` | Labels, secondary text |
| `Hairline` | `#DDD9CC` | `#2A2519` | Borders |

### Brand & caution
| Token | Light | Dark | Use |
|---|---|---|---|
| `PoolTeal` | `#1F6E7A` | `#64B9C3` | **Accent** — RH readout, selected chips, buttons, global tint |
| `PoolTealSunk` | `#165059` | `#3E8E99` | Pressed accent |
| `SignalAmber` | `#9A6A0B` | `#E8B84B` | Caution notes only (never a severity) |

### Fire-behavior severity ramp
Fills that carry white text; mapped from `BadwaterCore.FireBehavior`.
| Token | Light | Dark | PIG band (IRPG p.49) |
|---|---|---|---|
| `SeverityVeryLow` | `#5E7250` | `#6E8460` | < 10% — Very low |
| `SeverityLow` | `#8A7A2E` | `#B3A24A` | 10–20% — Low |
| `SeverityMedium` | `#B07A1E` | `#CE9636` | 20–30% — Medium |
| `SeverityHigh` | `#BE5F26` | `#D9823C` | 30–50% — High |
| `SeverityVeryHigh` | `#B0431F` | `#DD6A30` | 50–70% — Very high |
| `SeverityExtreme` | `#9E2B1B` | `#D6402A` | 80–100% — Extreme |

> **For Claude Design:** the severity ramp is the highest-value place to spend
> refinement time. It must read as one continuous green→red scale, stay
> distinguishable for color-vision deficiency (pair color with the text label —
> already done), and hold contrast for white text on both themes.

---

## 3. Typography

System faces only — **SF Pro** for text, **SF Mono** for tabular figures and
labels. Rounded design for the numeric readouts gives the instrument feel.

| Role | Spec |
|---|---|
| Big readout (PIG %, RH %) | SF Rounded, Heavy, monospaced digits, 46–72 pt |
| Input value | SF Rounded, Bold, mono digits, 32 pt |
| Title | SF Rounded, Bold, 22 pt |
| Body / controls | SF Rounded, body |
| Field label / chip / page ref | **SF Mono**, caption, uppercase, +0.6 kerning, `Muted` |

Tabular numerals everywhere digits change in place, so values don't jitter as
they update live.

---

## 4. Screens

Three tabs — **Humidity, Ignition, Obs** (left to right), launching on
**Ignition** (`App/BadwaterIgnition/App/RootView.swift`). Each is an
**instrument, not a form** — no "Calculate" button; results update on every
input change. All three tabs share a single `IgnitionModel`, so the weather
the Obs tab logs is the same weather Ignition shows. (The Obs tab is the
Weather-Watch feature — `WatchView` / `WeatherWatchModel` internally.)

> A pending workflow restructure (pinned result bar, grouped input sections,
> unified status strip) is specified in `docs/UX_WORKFLOW.md`.

### 4.1 Ignition (PIG / FFM)
Top-to-bottom:
1. **Header** — "Ignition" + `IRPG p.44–49` mono reference.
2. **Fast inputs** — Dry bulb °F plus a **Humidity source** chip row (`Direct` /
   `From wet bulb`). Direct shows a Rel. humidity % `StepperCard` (Kestrel); From
   wet bulb swaps in a Wet bulb °F `StepperCard` and an elevation-band chip row,
   and shows the derived RH read-only in pool teal — the value that flows into
   the chain. Big −/+ targets (≥ 48 pt) for gloved hands.
3. **Site factors** — chip rows: Month (shows resolved table B/C/D), Time of day
   (six bands + Night), Aspect, Slope, Elevation vs. weather site. These persist
   between launches; month/time pre-fill from the clock.
4. **Chain strip** — `REF FM → CORR → FFM`, showing the intermediate IRPG values
   so the result is verifiable against the printed guide. This is the trust
   feature.
5. **Results** — two `ResultCard`s side by side: **Unshaded** and **Shaded**,
   each a big PIG % in its severity color with a severity stripe.
6. **Interpretation** — plain-language fire behavior for the (more hazardous)
   unshaded PIG, always with the IRPG caution: *"Use these thresholds with
   caution; consult local experts."*
7. **Disclaimer** — decision support only; not affiliated with NWCG.

### 4.2 Humidity (RH / dew point)
1. **Header** — "Humidity" + `NWCG PMS 437`.
2. **Big RH readout** — 72 pt, pool teal (the brand moment).
3. **Inputs** — Dry bulb & Wet bulb stepper cards.
4. **Derived stats** — Dew point, Wet-bulb depression.
5. **Elevation band** — chip row (shows station pressure in inHg) + Alaska
   thresholds toggle.
6. **"Use in ignition calc"** — teal button; pushes RH into the Ignition tab and
   switches to it. Sling psychrometer → RH → PIG in one pipeline, no re-typing.

### 4.3 Obs (shift observation log)
1. **Header** — "Obs" + the shift's obs count (`IRPG PMS 461` before the
   first log).
2. **Latest-reading hero** — the last logged obs: time, both `ResultCard`s,
   and a radio-ready broadcast line ("Wx obs 1430, temp 90, RH 8, …").
3. **Broadcast panel** — the full frozen radio script for the latest obs, with
   a Share action for the radio operator.
4. **Trend** — a `Swift Charts` sparkline of unshaded PIG across the shift
   (shown once there are ≥2 observations).
5. **Shift log** — every logged obs (time · PIG · wind · note) with per-row
   delete and an undo affordance.
6. **Site & radio** — the sticky IMET header: addressee, location name,
   division, site elevation, and lat/long (typed, kept offline). The first log
   of a shift is gated on an explicit **Confirm site** review so a persisted
   default can never silently feed a broadcast.
7. **Pending capture card** — weather-freshness strip, obs-time steppers, the
   shared weather inputs (bound to the same `IgnitionModel` as Ignition,
   including wind), an optional note, and a live PIG preview.
8. **Export shift** — IMET `.xlsx` (via the file exporter), NWS spot obs, and a
   Notes table (via the share sheet).
9. **Log bar** — fixed at the bottom; freezes the pending reading (with wind)
   into the shift.

---

## 5. Components (built)
- `StepperCard` — label, big tabular value + unit, −/+ buttons.
- `StatCard` — read-only derived value.
- `ChipPicker` — horizontal single-select chips; selected = teal ring + tint.
- `ResultCard` — severity stripe + big PIG readout + FFM sub-line.

---

## 6. Field-conditions spec (non-negotiable)
- **Gloves:** interactive targets ≥ 48 pt.
- **Glare:** high contrast; test the light theme in direct sun.
- **Night ops:** near-black dark theme to protect dark adaptation.
- **Trust:** intermediate values always visible; every screen cites its IRPG /
  PMS page.
- **Offline & instant:** no network, no accounts, no analytics; cold-launch to a
  usable state; all inputs persisted.

---

## 7. Open for Claude Design
1. **App icon** — the Badwater pool-against-salt motif; `AppIcon.appiconset` is a
   placeholder. Consider a teal droplet/basin over a warm horizon.
2. **Severity ramp tuning** on real OLED and in sunlight.
3. **Motion** — subtle: a value tick on change, a severity-color cross-fade.
   Keep it minimal; respect Reduce Motion.
4. **"Book mode" (v2 candidate)** — render the actual IRPG table with the
   selected cells highlighted; the app *becomes* the pocket-guide page.
5. **Apple Watch app / widget (v2)** — glanceable PIG on the wrist or home
   screen during a weather obs. (Distinct from the shipped **Obs tab**,
   §4.3, which is the phone's shift observation log.)

---

## 8. Not affiliated
Badwater Ignition is an independent decision-support tool. It is **not affiliated
with or endorsed by the NWCG.** Source data: *Incident Response Pocket Guide*,
PMS 461, and NWCG temperature/RH/dew-point tables, PMS 437 — U.S. government
public-domain works.
